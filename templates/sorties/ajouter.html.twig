{% extends 'base.html.twig' %}

{% block title %}Ajouter une Sortie{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Ajouter une Sortie</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary mt-2">Enregistrer</button>
        <a href="{{ path('sorties_liste') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
        {{ form_end(form) }}
    </div>
{% endblock %}
